# Development Database
# Replace with your local PostgreSQL connection string
DEV_DATABASE_URL=postgresql://username:password@localhost:5432/server_panda_dev

# Production Database (for Fly.io deployment)
# This will be set automatically by Fly.io or replace with your production database URL
DATABASE_URL=postgresql://username:password@host:5432/server_panda_prod

# Server Configuration
PORT=3001
NODE_ENV=development

# Example for Fly.io hosted database development setup:
# If using Fly.io database with proxy, your DEV_DATABASE_URL might look like:
# DEV_DATABASE_URL=postgres://postgres:your_password@127.0.0.1:5432/postgres